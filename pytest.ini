[pytest]
testpaths        = tests
addopts          = -v --tb=short
log_cli          = false
log_level        = WARNING

markers =
    slow: marks tests as slow (deselect with -m "not slow")
    integration: full pipeline integration tests
